#ifndef UTILS_H
#define UTILS_H

#define VERBOSE
// #define WEIGHTED

#define SQR(x) ((x) * (x))
#define ABS(x) ((x) < 0 ? -(x) : (x))
#define MIN2(a, b) ((a) < (b) ? (a) : (b))
#define MAX2(a, b) ((a) > (b) ? (a) : (b))

#endif /* ! UTILS_H */
